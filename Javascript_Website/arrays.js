console.log("Hello");

//This is the section for the guest list

let list = [];

function guestList() {
    let guestName = document.getElementsById(frm1);
    list.push(guestName);
    console.log(list);
}

